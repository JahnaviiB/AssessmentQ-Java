/* This is basically a Javascript file*/
/*export interface Customer{
    id:number;
    firstname:string;
    lastname:string;
    address:string;
}
@injectable({
        providedIn:'root'
})

export class CustomerService{
    private baseURL='/api/customer';
    constructor(private http: HttpClient){}
    getCustomerById(id: number): observable<Customer>{
        return this.get<Customer>(`${this.baseUrl}/{id}`);
    }
}

@Component ({
        selector
})

//HTML code

<div *ngIf="customer">
<h2> Customer Info</h2>
<p><string>First Name:</string> {{customer.firstName}}</p>
<p><string>Last Name:</string> {{customer.lastName}}</p>
<p><string>address:</string> {{customer.address}}</p>
</div>

public class Customer {
    private Long id;
    private String firstname;
    private String lastname;
    private String address;

    @RestController
    @RequestMapping("/api/customers")
}*/

